
export function decodeDatum(rawDatumHex: string): number {
    // TODO: BÀI TẬP THỰC HÀNH!
    // Hãy giải mã chuỗi rawDatum (CBOR Hex) từ Game UTxO 
    // để xem được giá trị Secret Number.

    // Xóa dòng dưới đây và bắt đầu viết logic parse của bạn:
    throw new Error("TODO_DECODE_DATUM: Hãy implement hàm này");
}
